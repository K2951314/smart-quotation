fastapi>=0.128
uvicorn>=0.38
pydantic>=2.12
PyYAML>=6.0
openpyxl>=3.1       # Excel 文件上传解析（数据导入功能必须）
python-multipart>=0.0.9  # FastAPI 文件上传依赖
cryptography>=43.0   # AES-GCM 价格包加密（Bundle 生成功能必须）
requests>=2.31      # 三菱库存 GWT-RPC 查询引擎（HTTP 请求）
python-dotenv>=1.0   # 从 .env 文件加载环境变量（本地开发用）

# 错误监控（可选；设置 SENTRY_DSN 环境变量时自动启用）
sentry-sdk[fastapi]>=2.0

# 测试依赖（本地开发用，不入库）
# httpx>=0.27  # fastapi.testclient 需要
