# 2026-06-02 工作日志

## 完成：多租户 GUI 配置系统补全

基于 Codex 已实现的 v1 骨架，补全了设计方案中所有未完成模块：

### 后端 (store.py + api.py)
- 新增 `list_companies()` → `GET /api/companies`
- 新增 `list_configs(company_id)` → `GET /api/companies/{id}/configs`（版本历史）
- 新增 `rollback_config(company_id, revision)` → `POST /api/companies/{id}/config/{revision}/publish`
- 新增 `list_audit(company_id)` → `GET /api/companies/{id}/audit`
- 新增 `get_items_stats(company_id)` → `GET /api/companies/{id}/items/stats`

### GUI (admin/index.html + admin/app.js + admin/styles.css)
- **页面显示区（新增）**：app_title、result_layout 四区域（identity/metrics/chips/details）全 GUI 配置
- **定价设置区（新增）**：公式、小数位、取整模式、整数阈值
- **数据导入升级**：Tab 切换，JSON 输入 + 字段映射预览（标绿已定义字段）+ 数据统计栏
- **版本历史（新增）**：列出所有版本，状态徽章，一键回滚
- **审计日志（新增）**：最近 50 条操作记录
- **公司列表（新增）**：列出所有公司，点击芯片切换 company_id

### 测试
- test_backend_v1.py：从 4 条扩充到 12 条（新增回滚、版本历史、数据统计、公司列表、审计日志、配置验证边界 3 种）
- test_admin_gui.py：从 1 条扩充到 4 条（新增页面显示、历史/审计区、数据导入 Tab）
- **全部 16 条测试通过**

### 文档
- docs/multitenant-config-v1.md 全量更新，含完整 API 表格
- requirements.txt 补充 httpx 注释
